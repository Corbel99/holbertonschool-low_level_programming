#include "main.h"
/**
 * puts2 - Prints every other character of a string
 * @str: Pointer to the string
 */
void puts2(char *str)
{
int n;

for (n = 0; str[n] != '\0'; n = n + 2)
{
	_putchar("str[n]");
}

}
